import {PullRequestVirConfig} from './pull-request-vir-config';

describe('PullRequestVirConfig', () => {
    it('allows an empty object', () => {
        const testAssignment: PullRequestVirConfig = {};
    });
});
