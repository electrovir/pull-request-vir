import {PullRequestVirConfig} from '../src/config/pull-request-vir-config';

const config: PullRequestVirConfig = {
    applyFormatting: {
        command: 'npm run format',
    },
};

export default config;
